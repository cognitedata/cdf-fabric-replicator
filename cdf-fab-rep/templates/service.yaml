apiVersion: v1
kind: Service
metadata:
  name: {{ include "cdf-fab-rep.fullname" . }}
  labels:
    {{- include "cdf-fab-rep.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cdf-fab-rep.selectorLabels" . | nindent 4 }}
