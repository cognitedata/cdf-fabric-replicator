apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cdf-fab-rep.fullname" . }}-test-connection"
  labels:
    {{- include "cdf-fab-rep.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cdf-fab-rep.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
