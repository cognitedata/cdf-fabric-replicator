{
    "$id": "event_config.schema.json",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "description": "CDF Event configuration",
    "unevaluatedProperties": false,
    "properties": {
        "lakehouse-abfss-path-events": {
            "type": "string",
            "description": "Input the path to the table in the lakehouse to store CDF events."
        },
        "batch-size": {
            "type": "integer",
            "description": "Input the number of events to read in a single batch from CDF."
        }
    }
}