{
    "$id": "raw_config.schema.json",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "description": "CDF RAW configuration",
    "unevaluatedProperties": false,
    "properties": {
        "table-name": {
            "type": "string",
            "description": "Enter the name of the RAW table in CDF to store rows."
        },
        "db-name": {
            "type": "string",
            "description": "Enter the database name in CDF to store the row."
        },
        "raw-path": {
            "type": "string",
            "description": "Input the subpath in the lakehouse to read rows."
        },
        "incremental-field": {
            "type": "string",
            "description": "Input the field for incremental loading. This value is normally a timestamp."
        }
    }
}