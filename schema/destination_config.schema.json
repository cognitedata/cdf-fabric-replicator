{
    "$id": "destination_config.schema.json",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "description": "CDF time series configuration",
    "unevaluatedProperties": false,
    "properties": {
        "time-series-prefix": {
            "type": "string",
            "description": "Enter the prefix to add to CDF time series external IDs created from Fabric."
        }
    }
}