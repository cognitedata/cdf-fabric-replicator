{
    "$id": "raw_tables_config.schema.json",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "description": "CDF RAW configuration for to be synced to Fabric",
    "unevaluatedProperties": false,
    "properties": {
        "table-name": {
            "type": "string",
            "description": "Enter the name of the RAW table in CDF to sync to Fabric."
        },
        "db-name": {
            "type": "string",
            "description": "Enter the database name in CDF to sync to Fabric."
        },
        "lakehouse-abfss-path-raw": {
            "type": "string",
            "description": "Enter the full ABFFS path of the table to store RAW rows into."
        }
    }
}