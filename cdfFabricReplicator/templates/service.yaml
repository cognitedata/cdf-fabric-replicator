apiVersion: v1
kind: Service
metadata:
  name: {{ include "cdfFabricReplicator.fullname" . }}
  labels:
    {{- include "cdfFabricReplicator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cdfFabricReplicator.selectorLabels" . | nindent 4 }}
